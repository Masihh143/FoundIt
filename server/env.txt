# ====== REQUIRED (both local and Render) ======
# MongoDB connection string
dbUrl=YOUR_MONGODB_URI

# JWT secret for auth
JWT_SECRET=YOUR_RANDOM_JWT_SECRET

# Cloudinary credentials
CLOUD_NAME=YOUR_CLOUDINARY_CLOUD_NAME
CLOUD_API_KEY=YOUR_CLOUDINARY_API_KEY
CLOUD_API_SECRET=YOUR_CLOUDINARY_API_SECRET

# Environment
NODE_ENV=production

# ====== OPTIONAL ======
# Render provides PORT automatically; leave unset locally if you want 5000 fallback
# PORT=5000

# Used only in local development for CORS (ignored in production)
# CLIENT_URL=http://localhost:5173

# Frontend uses this at build time; on Render we default to /api so it's optional
# VITE_API_BASE_URL=/api